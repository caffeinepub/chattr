{
  "schemaVersion": "1.0.0",
  "projectStateVersion": 3,
  "summary": "Project involves a lobby that should show rooms on first load; production still appears to apply filtering too early or is serving a stale cached bundle, causing the fix not to take effect.",
  "architectural_notes": [
    "Implement reliable cache-busting/versioning for production assets (e.g., hashed filenames and/or CDN invalidation) to prevent stale bundles after deployments."
  ],
  "known_issues": [
    "Production deployment may still be serving a stale cached bundle, so lobby filtering fixes are not reflected live (rooms still not shown on first load)."
  ],
  "isFixRequest": true,
  "existing_features": [
    {
      "id": "feature-implement-a-deterministic-cache-busting-change-in-frontend-index-html-so-production-clients-cdns-fetch-the-newest-frontend-bundle-instead-of-a-stale-cached-copy-ensuring-the-lobby-s-corrected-first-load-behavior-rooms-visible-with-no-category-selected-is-what-users-receive",
      "title": "Implement a deterministic cache-busting change in `frontend/index.html` so production clients/CDNs fetch the newest frontend bundle instead of a stale cached copy, ensuring the lobby’s corrected first-load behavior (rooms visible with no category selected) is what users receive.",
      "reqIds": [
        "REQ-1"
      ],
      "version": 1
    }
  ],
  "new_feature_requests": [
    {
      "id": "AR-1",
      "summary": "Force CDN cache-busting so production definitely serves the latest bundle with the corrected lobby filtering logic",
      "reqIds": [],
      "status": "in_progress"
    }
  ],
  "projectName": "Force production cache-bust so the latest lobby bundle is served",
  "clarification_mode": "instant",
  "clarification_rounds": 0
}