{
  "kind": "build_request",
  "title": "Ensure category tags are lowercase in chatrooms",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Convert category display text to lowercase in the ChatroomPage component where category tags are rendered",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6"
        ],
        "quotes": [
          "make sure category tags are lowercase in chatrooms as well."
        ]
      },
      "acceptanceCriteria": [
        "Category tags displayed in the chatroom view show lowercase text",
        "Styling matches the existing LIVE badge format (padding, font size)"
      ]
    }
  ],
  "constraints": [
    "Do not modify ChatroomCard.tsx or LobbyPage.tsx as they already implement lowercase categories",
    "Maintain consistent styling with existing category tag implementation"
  ],
  "nonGoals": [
    "Changing backend category storage format",
    "Modifying category selection in CreateChatroomDialog"
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}